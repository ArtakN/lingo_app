/*
================================================================================
Beginner

   script tag
   DOM
   variables
      initialization / assignment
      reassignment
   numbers
      increment
      decrement
      toLocaleString()
      toFix()
   strings
      concatenate
      template strings  ``
      strings vs numbers
      emoji is string
   innerText
   textContent
   booleans
      true
      false
   null
   undefined
   console.log()
   getElementById("")
   querySelector
      id
      class
      tag
      tag with an attribute
   array
      length
      push() & unshift()
      pop() & shift()
      splice()
      slice()
      find()
      map()
      filter()
      sort()
      reduce()
      trim()
      clear old arrray
   for loop 
      iterate array with for loop
   assignment operator
      =, +=, *=
   arithmetic operators
      +, -, *, /, **, %
   comparison operators
      ===, !==
      >, <, >=, <=
   if else statements
   logical operators
      &&
      ||
      !
   functions
      return
      parameters & arguments
      universal, reusable functions
   objects
      key: value
      properties
      methods
      acces a velue of the object
         that notation
         bracket notation
      objects in array
   event listener
      add event listener on a button
         -in HTML
         -in JavaScript 
      event types
         click
         double click
         mouse hover
         submit
   Math object
      .random() method
      .floor() method
   add element in document
      innerHTML
      createElement() & append() methods
   truthy and falsy values
      Boolean()
================================================================================

================================================================================
Intermediate

   timing
      setTimeout()
      current time
      setInterval()
   element.attribute
      style attribute
      disabled attribute
      hide an element
   forms
      submitting form
      e.preventDefault()
      input value
      textarea value
      radio inputs
      checkbox input
   formData
      storing data
      getting data
   CSS: row-reverse
   classList object
      .add()
      .remove()
      .toggle()
      .contains()
   getElementByClassName(e.target.id)
   parentElement
   for of
      iterate array
      iterate nested arrray
   element.children
   getElementsByClassName
      getElementsByClassName() vs querySelectorAll()
   import & export
      export directly
      export by default
      export functions
      export constructor funtions
   array
      .includes()
      .filter()
   forEach(function(){})
      iterate array
      iterate nested arrray
      second parameter
   CDNs
      add icons
      generate UUIDs
   data attributes
   operator NOT (!)
   flip a boolean
   conditonaly render styles
   local varibles
================================================================================

================================================================================
Advanced

   copying objects
   destructuring objects
   .map() method
   .join() method
   return function in function
   new Array constructor
   .fill() method
   chaining methods together
   constructor functions
   Object.assign()
   .reduce() method
   ternary operator
   arrow functions
   callback functions
   Classes
================================================================================

================================================================================
APIs and Async JS

   API
   JSON
   fetch
      .then()
   HTTP requests
      URLs, endpoints
      methods
      body
      headers
   reset form
   REST APIs
      resources
      parameters
      Statelessness
      queries
   Promises
      states
      chaning
      .catch()
      throw
      status
      error
   Async/Await
   Axios
      async-await for axios
      try/catch
   localStorage
      .setItem()
      .getItem
      .clear()
   array in localStorage
      JSON.stringify()
      JSON.parse()
================================================================================
*/