/* 
   Why React
      declarative
      composible
   JSX
      className
      single parent element
      close all the tags
      JSX returns JS objects
   Components
      nesting components
      organize componenets
      definition
      return statements 
      root component
   Export & import
      default
      named
      mix   
      import image
   Images in react
      import image via src folder
      use image via public folder
   JS in JSX
      {{}}
   Styles
      className
      inline styles
      css modules
      scss in react
   Render
   Props
      destucturing props
      default value for a prop
      non-string props
      children prop
      props immutable
   Spread operator
   Mapping data
      key property
   Event listeners
      in Js
      in Html
      in React
         onClick
         onChange
         onSubmit
         onMoueEnter
   State
      setting state
      changing state
      array state
      object state
      state as props
      state sructure
   Dynamic styles
      styles in JSX element
      dynamic styles
   Conditional rendering
      &&
      ternary operator
      if else statement
      conditionally returning null
   Forms
      event object
      controlled component
   API
   Side effects
   Hooks
      useState()
      useEffect()
      useContext()
         Create Context
         Use Context
      useRef()
      useCallback()
      useMemo()
      useNavigate()
*/